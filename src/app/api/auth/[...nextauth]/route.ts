// eslint-disable-next-line @typescript-eslint/no-unused-vars
import { authConfig } from "@/auth/auth-options";
import { handlers } from "@/auth/auth-options";

export const { GET, POST } = handlers;
